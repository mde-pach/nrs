# Billing Domain

Invoices are created on the first business day of every month.
Each invoice aggregates all the billable events from the previous period.
Failed payment attempts trigger automatic retry with exponential backoff.
