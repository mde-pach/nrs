# Project

## Billing

Invoices are generated on the first business day of each month.
Each invoice aggregates all billable events from the prior period.
Failed payment attempts trigger an automatic retry with exponential backoff.

## Commands

- `make build` — build
