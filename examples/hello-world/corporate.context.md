# Corporate Context

## Standards

- TypeScript strict mode — no `any`, no `ts-ignore`
- Formatter (Biome) and linter run on precommit
- All PRs require passing CI before merge
- Gitmoji convention for commits
